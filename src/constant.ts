export const projectsList: string[] = [
  'Stinky Clock',
]

export interface ITheme {
  iconDefault: string
  iconStart: string
  iconFinish: string
  audioReady: string
  audioStart: string
  audioCancel: string
}
